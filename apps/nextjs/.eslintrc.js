module.exports = {
  root: true,
  // By adding custom to our extends array, we're telling ESLint to look
  // for a package called eslint-config-custom - and it finds our workspace.
  extends: ['custom'],
};
